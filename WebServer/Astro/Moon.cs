using CosineKitty;

namespace WebServer.Astro
{
    public class Moon
    {
        public static double GetMoonPhase(AstroTime time)
        {

            // Calculate the moon phase based on the provided AstroTime
            // This is a placeholder implementation; replace with actual logic
            return Astronomy.MoonPhase(time);
        }

    }
}
